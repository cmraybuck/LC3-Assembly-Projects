# XORME.ASM
Takes two binary inputs from the user. Outputs the result of performing an XOR operation on the two strings of binary.

# LETTER.ASM
Displays "NCSU" in PennSim and changes color depending on button inputs from user.

# COWTIP.ASM
Displays pixel art of a cow and moves around based on user input (WASD). 
